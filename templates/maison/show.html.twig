{% extends 'base.html.twig' %}

{% block title %}Maison{% endblock %}

{% block body %}

    <h1>Maison</h1>
    <div id="carouselExampleInterval" class="carousel slide" data-ride="carousel">
        <div class="carousel-inner">
            <div class="carousel-item active" data-interval="10000">
                <img src="{{ vich_uploader_asset(maison, 'imageFile') }}" class="d-block w-100" alt="...">
            </div>
            <div class="carousel-item" data-interval="2000">
                <img src="{{ vich_uploader_asset(maison, 'imageFile') }}" class="d-block w-100" alt="...">
            </div>
            <div class="carousel-item">
                <img src="{{ vich_uploader_asset(maison, 'imageFile') }}" class="d-block w-100" alt="...">
            </div>
        </div>
        <a class="carousel-control-prev" href="#carouselExampleInterval" role="button" data-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Previous</span>
        </a>
        <a class="carousel-control-next" href="#carouselExampleInterval" role="button" data-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Next</span>
        </a>
    </div>


    <table class="table">
        <tbody>
            <tr>
                <th><h2>Prix</h2></th>
                <td>{{ maison.prix }}</td>
            </tr>
            <tr>
                <th>Gouvernorat</th>
                <td>{{ maison.getGouvernorat.title }}</td>
            </tr>
            <tr>
                <th>Région</th>
                <td>{{ maison.region.title }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ maison.type.title }}</td>
            </tr>
            <tr>
                <th>Etage</th>
                <td>{{ maison.etage }}</td>
            </tr>
            <tr>
                <th>Clima</th>
                <td>{{ maison.clima ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>CreateAt</th>
                <td>{{ maison.createAt ? maison.createAt|date('d-m-Y') : '' }}</td>
            </tr>
        </tbody>
        <tfoot>
        <th>File</th>
        <td> <a href="{{ vich_uploader_asset(maison, 'imageFile') }}" download> Download </a> </td>
        </tfoot>
    </table>

    <a href="{{ path('maison_index') }}">back to list</a>

    <a href="{{ path('maison_edit', {'id': maison.id}) }}">edit</a>

    {{ include('maison/_delete_form.html.twig') }}
{% endblock %}
